# Core dependencies
# numpy 1.24.x is required: 1.20.0 lacks numpy.typing.NDArray and breaks
# torchvision 0.18.1 import.
numpy==1.24.4
onnx
# onnxruntime must support ONNX opset 18 (models here are exported with it).
# 1.10.0 only supports up to opset 15; 1.19.2 is the last release for Python 3.8.
onnxruntime>=1.16
mujoco
hydra-core
omegaconf
pygame
scipy
pyyaml

# Real-robot deployment (DDS communication)
# Install via: pip install cyclonedds
cyclonedds

# PyTorch — install via conda (see README for exact command):
#   conda install pytorch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 pytorch-cuda=12.1 -c pytorch -c nvidia

# Unitree SDK — install via git clone (see README):
#   git clone https://github.com/unitreerobotics/unitree_sdk2_python.git
#   cd unitree_sdk2_python && pip install -e .

# Onboard perception node only (ROS2, not pip-installable):
#   rclpy, livox_ros_driver2, unitree_hg

# Model weights and compiled binaries — do NOT commit
# These files are either auto-downloaded by ultralytics or must be
# compiled locally because TRT .engine files are hardware-specific.
*.pt
*.onnx
*.engine

# Allow scripts and documentation
!*.sh
!*.md
!.gitignore
